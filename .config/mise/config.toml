[tools]
go = "latest"
node = "24"
bun = "latest"
python = "3.14.0"
rust = "latest"
java = "latest"

"npm:typescript" = "latest"
"npm:prettier" = "latest"
"npm:pnpm" = "latest-10"

"cargo:eza" = "latest"
"cargo:sheldon" = "0.8.5"       # Shell plugin manager (was rossmacarthur/sheldon)
"cargo:viddy" = "1.3.0"         # Modern watch command (was sachaos/viddy)
"cargo:procs" = "0.14.10"       # Modern ps replacement (was dalance/procs)

"cargo:https://github.com/openai/codex.git" = { version = "tag:rust-v0.56.0-alpha.5", crate = "codex-cli" }

"npm:@google/gemini-cli" = "latest"

# Claude Code
"npm:ccusage" = "latest"
"pipx:claude-monitor" = "latest"

# Git diffs
"npm:difit" = "latest"
"cargo:git-delta" = "latest"

"cargo:similarity-ts" = "latest"
"cargo:similarity-generic" = "latest"

# Migrated from aqua
# CLI tools
gh = "2.83.0"                    # GitHub CLI (was cli/cli)
fzf = "0.66.1"                   # Fuzzy finder (was junegunn/fzf)
kubectl = "1.34.1"               # Kubernetes CLI (was kubernetes/kubectl)
bat = "0.26.0"                   # Cat clone with syntax highlighting (was sharkdp/bat)
golangci-lint = "2.6.1"          # Go linter (was golangci/golangci-lint)
helm = "3.19.0"                  # Kubernetes package manager (was helm/helm)
helmfile = "1.1.9"               # Declarative Helm charts (was helmfile/helmfile)
terraform = "1.13.5"             # Infrastructure as Code (was hashicorp/terraform)
kustomize = "5.7.1"              # Kubernetes config management (was kubernetes-sigs/kustomize)
ripgrep = "15.1.0"               # Fast grep alternative (was BurntSushi/ripgrep)
fd = "10.3.0"                    # Fast find alternative (was sharkdp/fd)
kind = "0.30.0"                  # Kubernetes in Docker (was kubernetes-sigs/kind)
shellcheck = "0.11.0"            # Shell script linter (was koalaman/shellcheck)
yamlfmt = "0.20.0"               # YAML formatter (was google/yamlfmt)
actionlint = "1.7.8"             # GitHub Actions linter (was rhysd/actionlint)
starship = "1.24.0"              # Cross-shell prompt (was starship/starship)
jq = "1.8.1"                     # JSON processor (was jqlang/jq)
neovim = "0.11.5"                # Vim-fork text editor (was neovim/neovim)
trivy = "0.67.2"                 # Security scanner (was aquasecurity/trivy)
awscli = "2.31.31"               # AWS CLI (was aws/aws-cli)
yq = "4.48.1"                    # YAML processor (was mikefarah/yq)
ghq = "1.8.0"                    # Repository management tool (was x-motemen/ghq)
kubetail = "1.6.21"              # Tail Kubernetes logs (was johanhaleby/kubetail)
checkmake = "0.2.2"              # Makefile linter (was mrtazz/checkmake)
gcloud = "546.0.0"               # Google Cloud SDK (was twistedpair/google-cloud-sdk)
docker-cli = "28.3.2"            # Docker CLI (was docker/cli)

